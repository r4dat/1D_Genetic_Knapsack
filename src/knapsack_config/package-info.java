/**
 * Package containing binary knapsack configuration variables. 
 * List of object weights, values, and knapsack capacity. 
 * Weights and values are ordered (e.g. Weights[0] corresponds to 
 * Values[0].
 */
package knapsack_config;